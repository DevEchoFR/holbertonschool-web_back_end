#!/usr/bin/env python3
"""Module that defines a function to concatenate two strings."""


def concat(str1: str, str2: str) -> str:
    """Return a new string made by concatenating two input strings."""
    return str1 + str2
