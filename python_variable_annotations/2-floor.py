#!/usr/bin/env python3
"""Module that defines a function to compute the floor of a float."""

import math


def floor(n: float) -> int:
    """Return the greatest integer less than or equal to the input float."""
    return math.floor(n)
